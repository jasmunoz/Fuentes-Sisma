ps -fe | grep rtratami | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rtrataan | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rtratawp | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio01 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio02 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio04 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep01 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep02 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep04 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep rdif0301 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rdif0501 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rdif0701 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep penvio03 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio05 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio07 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep precep03 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep05 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep07 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep pmanej03 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep pmanej05 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep pmanej07 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep ptimer03 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep ptimer05 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep ptimer07 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio30 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio40 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio20 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep penvio10 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep30 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep40 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep20 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep precep10 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep ptimer30 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep ptimer40 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep ptimer20 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep ptimer10 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl3002en | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl4002en | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl2002en | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl1002en | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl3002re | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep cl4002re | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl2002re | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep cl1002re | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rdif3002 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rdif4002 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done

ps -fe | grep rdif2002 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto 
  kill -9 $pid
done

ps -fe | grep rdif1002 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto 
  kill -9 $pid
done

ps -fe | grep cl3004en | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep cl3004re | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid
done


ps -fe | grep rdif3004 | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid 
done


ps -fe | grep superviI | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid 
done

ps -fe | grep superviA | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid 
done

ps -fe | grep superviW | grep $USER | grep -v "grep" | while read usuario pid resto
do
  echo $usuario cat $pid cat $resto
  kill -9 $pid 
done






